{
  "name": "Log-Analyser",
  "version": "1.0.0",
  "description": "You are given a huge log file (which may be gigabytes or terabytes) generated by some application. Please see the attached file for a 5MB extract. There are a lot of lines, but each line is fairly short (up to 256 bytes) and starts with a timestamp followed by some text.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "express": "^4.17.1"
  }
}
